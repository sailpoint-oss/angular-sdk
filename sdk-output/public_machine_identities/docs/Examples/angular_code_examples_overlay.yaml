- path: /public-machine-identities/v1
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/public_machine_identities/methods/public-machine-identities#list-public-machine-identities-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { PublicMachineIdentitiesService } from '@sailpoint/angular-sdk/public_machine_identities';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(PublicMachineIdentitiesService);

          listPublicMachineIdentitiesV1(): void {
            const limit: number = 250; // number
            const offset: number = 0; // number
            const count: boolean = true; // boolean
            const filters: string = 'name eq "Production API Agent"'; // string
            const sorters: string = 'name'; // string
            this.api.listPublicMachineIdentitiesV1({  }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
