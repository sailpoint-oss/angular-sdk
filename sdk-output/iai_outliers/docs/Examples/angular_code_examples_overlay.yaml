- path: /outliers/v1/export
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#export-outliers-zip-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          exportOutliersZipV1(): void {
            const type: string = 'LOW_SIMILARITY'; // 'LOW_SIMILARITY' | 'STRUCTURAL'
            const xSailPointExperimental: string = 'true'; // string
            this.api.exportOutliersZipV1({  }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outlier-summaries/v1
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#get-identity-outlier-snapshots-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          getIdentityOutlierSnapshotsV1(): void {
            const limit: number = 250; // number
            const offset: number = 0; // number
            const type: string = 'LOW_SIMILARITY'; // 'LOW_SIMILARITY' | 'STRUCTURAL'
            const filters: string = 'snapshotDate ge "2022-02-07T20:13:29.356648026Z"'; // string
            const sorters: string = 'snapshotDate'; // string
            const xSailPointExperimental: string = 'true'; // string
            this.api.getIdentityOutlierSnapshotsV1({  }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outliers/v1
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#get-identity-outliers-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          getIdentityOutliersV1(): void {
            const limit: number = 250; // number
            const offset: number = 0; // number
            const count: boolean = true; // boolean
            const type: string = 'LOW_SIMILARITY'; // 'LOW_SIMILARITY' | 'STRUCTURAL'
            const filters: string = 'attributes.displayName sw "John" and certStatus eq "false"'; // string
            const sorters: string = 'attributes.displayName,firstDetectionDate,-score'; // string
            const xSailPointExperimental: string = 'true'; // string
            this.api.getIdentityOutliersV1({  }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outlier-summaries/v1/latest
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#get-latest-identity-outlier-snapshots-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          getLatestIdentityOutlierSnapshotsV1(): void {
            const type: string = 'LOW_SIMILARITY'; // 'LOW_SIMILARITY' | 'STRUCTURAL'
            const xSailPointExperimental: string = 'true'; // string
            this.api.getLatestIdentityOutlierSnapshotsV1({  }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outlier-feature-summaries/v1/{outlierFeatureId}
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#get-outlier-contributing-feature-summary-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          getOutlierContributingFeatureSummaryV1(): void {
            const outlierFeatureId: string = '04654b66-7561-4090-94f9-abee0722a1af'; // string
            const xSailPointExperimental: string = 'true'; // string
            this.api.getOutlierContributingFeatureSummaryV1({ outlierFeatureId: outlierFeatureId }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outliers/v1/{outlierId}/contributing-features
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#get-peer-group-outliers-contributing-features-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          getPeerGroupOutliersContributingFeaturesV1(): void {
            const outlierId: string = '2c918085842e69ae018432d22ccb212f'; // string
            const limit: number = 250; // number
            const offset: number = 0; // number
            const count: boolean = true; // boolean
            const includeTranslationMessages: string = 'include-translation-messages='; // string
            const sorters: string = 'importance'; // string
            const xSailPointExperimental: string = 'true'; // string
            this.api.getPeerGroupOutliersContributingFeaturesV1({ outlierId: outlierId }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outliers/v1/ignore
  method: post
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#ignore-identity-outliers-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          ignoreIdentityOutliersV1(): void {
            const requestBody: Array<string> = ; // Array<string>
            const xSailPointExperimental: string = 'true'; // string
            this.api.ignoreIdentityOutliersV1({ requestBody: requestBody }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outliers/v1/{outlierId}/feature-details/{contributingFeatureName}/access-items
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#list-outliers-contributing-feature-access-items-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          listOutliersContributingFeatureAccessItemsV1(): void {
            const outlierId: string = '2c918085842e69ae018432d22ccb212f'; // string
            const contributingFeatureName: string = 'entitlement_count'; // 'radical_entitlement_count' | 'entitlement_count' | 'max_jaccard_similarity' | 'mean_max_bundle_concurrency' | 'single_entitlement_bundle_count' | 'peerless_score'
            const limit: number = 250; // number
            const offset: number = 0; // number
            const count: boolean = true; // boolean
            const accessType: string = 'ENTITLEMENT'; // string
            const sorters: string = 'displayName'; // string
            const xSailPointExperimental: string = 'true'; // string
            this.api.listOutliersContributingFeatureAccessItemsV1({ outlierId: outlierId, contributingFeatureName: contributingFeatureName }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /outliers/v1/unignore
  method: post
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/iai_outliers/methods/iai-outliers#un-ignore-identity-outliers-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { IAIOutliersService } from '@sailpoint/angular-sdk/iai_outliers';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(IAIOutliersService);

          unIgnoreIdentityOutliersV1(): void {
            const requestBody: Array<string> = ; // Array<string>
            const xSailPointExperimental: string = 'true'; // string
            this.api.unIgnoreIdentityOutliersV1({ requestBody: requestBody }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
