- path: /transforms/v1
  method: post
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/transforms/methods/transforms#create-transform-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { TransformsService } from 'sailpoint-angular-sdk/transforms';
        import { Transform } from 'sailpoint-angular-sdk/transforms';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(TransformsService);

          createTransformV1(): void {
            const transform: Transform = {
              "name" : "Timestamp To Date",
              "attributes" : "{}",
              "type" : "dateFormat"
            }; // Transform
            this.api.createTransformV1({ transform: transform }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /transforms/v1/{id}
  method: delete
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/transforms/methods/transforms#delete-transform-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { TransformsService } from 'sailpoint-angular-sdk/transforms';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(TransformsService);

          deleteTransformV1(): void {
            const id: string = '2cd78adghjkja34jh2b1hkjhasuecd'; // string
            this.api.deleteTransformV1({ id: id }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /transforms/v1/{id}
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/transforms/methods/transforms#get-transform-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { TransformsService } from 'sailpoint-angular-sdk/transforms';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(TransformsService);

          getTransformV1(): void {
            const id: string = '2cd78adghjkja34jh2b1hkjhasuecd'; // string
            this.api.getTransformV1({ id: id }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /transforms/v1
  method: get
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/transforms/methods/transforms#list-transforms-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { TransformsService } from 'sailpoint-angular-sdk/transforms';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(TransformsService);

          listTransformsV1(): void {
            const offset: number = 0; // number
            const limit: number = 250; // number
            const count: boolean = true; // boolean
            const name: string = 'ExampleTransformName123'; // string
            const filters: string = 'name eq "Uppercase"'; // string
            this.api.listTransformsV1({  }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
- path: /transforms/v1/{id}
  method: put
  xCodeSample:
    - lang: Angular
      label: SDK_tools/sdk/angular/transforms/methods/transforms#update-transform-v1
      source: |
        import { Component, inject } from '@angular/core';
        import { TransformsService } from 'sailpoint-angular-sdk/transforms';
        import { Transform } from 'sailpoint-angular-sdk/transforms';

        @Component({ selector: 'app-example', template: '' })
        export class ExampleComponent {
          private readonly api = inject(TransformsService);

          updateTransformV1(): void {
            const id: string = '2cd78adghjkja34jh2b1hkjhasuecd'; // string
            const transform: Transform = {
              "name" : "Timestamp To Date",
              "attributes" : "{}",
              "type" : "dateFormat"
            }; // Transform
            this.api.updateTransformV1({ id: id }).subscribe({
              next: (result) => console.log(result),
              error: (error) => console.error(error),
            });
          }
        }
