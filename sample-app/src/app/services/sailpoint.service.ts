/**
 * Thin wrapper used by the sample app's config page.
 * It delegates all state management to the SDK's SailPointConfigService.
 */
export { SailPointConfigService as SailPointService } from '@sailpoint/angular-sdk';
export type { SailPointParams as SailPointConfig } from '@sailpoint/angular-sdk';
